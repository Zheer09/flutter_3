import 'package:flutter/material.dart';

class Bartodo extends StatelessWidget {
  const Bartodo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
