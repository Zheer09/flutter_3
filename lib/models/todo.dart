import 'package:exercise3/models/task.dart';

import 'package:exercise3/models/task.dart';

class todo {
  String title;
  List<task> tasks;

  int get perc {
    return perc;
  }

  todo(this.title, this.tasks);

  update(todo t) {
    this.title = t.title;
    this.tasks = t.tasks;
    perc;
  }
}
